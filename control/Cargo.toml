[package]
name = "control"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol", features = ["tokio"] }
eyre = "0.6"
tokio-serial = { version = "5.4", features = ["codec"] }
futures = "0.3"

[dev-dependencies]
